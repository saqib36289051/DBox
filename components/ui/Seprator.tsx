import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const Seprator = (props: Props) => {
    return (
        <View className="w-full h-[1px] bg-gray-400 my-2">
        </View>
    )
}

export default Seprator

const styles = StyleSheet.create({})